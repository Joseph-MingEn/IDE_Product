# Media removed from this mirror

`cline_kanban_demo.mp4` and `cline_kanban_demo.webm` were Git LFS objects unavailable on the remote. They were removed so pushes succeed.

See upstream [Cline](https://github.com/cline/cline) for originals.
