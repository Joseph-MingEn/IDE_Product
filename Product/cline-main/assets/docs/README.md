# Media removed from this mirror

`demo.gif` was tracked with Git LFS but the LFS object was missing on the remote (GitHub GH008). It was removed so this repository can push cleanly.

To restore assets, clone the upstream [Cline](https://github.com/cline/cline) repository separately.
